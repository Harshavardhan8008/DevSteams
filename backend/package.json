{
	"name": "devstream",
	"version": "1.0.0",
	"type": "module",
	"description": "Creating this project to use all my skills into use and learning all concepts in depth",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node --watch-path=. src/server.js"
	},
	"author": "Maharsha Vardhan Nakkella",
	"license": "ISC",
	"devDependencies": {
		"prettier": "^3.6.2"
	},
	"dependencies": {
		"dotenv": "^17.2.3",
		"express": "^5.1.0"
	}
}
